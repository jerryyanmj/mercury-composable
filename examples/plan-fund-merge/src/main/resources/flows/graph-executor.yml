flow:
  id: 'graph-executor'
  description: 'Route POST /api/graph/{graph_id} directly to the graph executor — no business logic here'
  ttl: 30s
  exception: 'graph.exception.handler'

first.task: 'graph.executor'

tasks:
  - input:
      - 'model.instance -> header.instance'
      - 'input.path_parameter.graph_id -> header.graph'
    process: 'graph.executor'
    description: 'Execute the named graph with the request body as input'
    output:
      - 'status -> output.status'
      - 'header -> output.header'
      - 'result -> output.body'
    execution: end

  - input:
      - 'error.code -> status'
      - 'error.message -> message'
      - 'error.stack -> stack'
    process: 'graph.exception.handler'
    description: 'Return a structured error response'
    output:
      - 'result.status -> output.status'
      - 'result -> output.body'
    execution: end
