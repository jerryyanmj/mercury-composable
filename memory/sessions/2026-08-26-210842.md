# Session 2026-08-26T21:08:42.000Z

**Agent:** Claude Code

## Summary

Continued the `examples/order-system-mongodb/` implementation from the prior session. All apps were running; picked up at the FAILED scenario investigation.

### Bugs found and fixed this session

**1. `SorProcess.java` — missing `cid` header + fire-and-forget requests**
- `v1.sor.mock.process` auto-emits three SoR milestones when it receives `sor.dispatch`. The emitted Kafka notifications were missing `.setHeader("cid", fulfillmentId)` (so the business CID wasn't on the wire) and called `po.request(...)` without `.get()` (fire-and-forget, no delivery guarantee).
- Fixed: extracted `fulfillmentId` from the already-present `input.get("fulfillment_id")` field; added `.setHeader("cid", fulfillmentId)` and `.get()` to each milestone notification.
- File: `order-external-mock/src/main/java/…/functions/SorProcess.java`

**2. `OrderPersist.java` — no `lines` array on order creation**
- `OrderAggregate`'s `$map` pipeline updates `line_status` on existing entries in the `lines` array, but `OrderPersist` never initialized that array. Result: `lines` was always `null`, `deriveOrderStatus(null)` returned `null`, and `order_status` was permanently stuck at `"PENDING"` — the CQRS read model never reflected SETTLED or FAILED.
- Fixed: added an `initialLine = {fulfillment_id, line_status: "PENDING"}` entry to `lines` in the `$setOnInsert` document.
- File: `order-manager/src/main/java/…/functions/OrderPersist.java`

**3. `OrderStatus.java` — internal `lines` field leaked to API response**
- After fixing the `lines` array, stripped it from the public status response (like `outbox`).
- File: `order-manager/src/main/java/…/functions/OrderStatus.java`

### DEMO mode for FAILED scenario testing

The external-mock's `SorProcess` respects `Demo.suppressed(headers)`. When `DEMO_MODE=true` is set on the external-mock process, it HOLDS milestones on `sor.dispatch` and requires manual emit via `POST /api/admin/sor/emit/{sor_ref}/{milestone}/{outcome}`. Restarted external-mock with `DEMO_MODE=true` to enable controlled FAILED testing.

### End-to-end verification

Both scenarios verified with the rebuilt code:

**SETTLED path** (all pass):
- Order submitted → manager relay → steward SCHEDULED → admin dispatch → steward DISPATCHED → steward relay → external-mock holds (DEMO) → manual emit all 3 pass → ack app processes → steward records SETTLED → steward relay → manager reads model: `order_status: "COMPLETED"`, history: VALIDATED→SCHEDULED→DISPATCHED→SETTLED

**FAILED path** (validated=fail):
- Same flow → manual emit validated=fail, preprocessed=pass, processed=pass → steward derives `progress=failed` at 1-of-3 in → lifecycle graph transitions to FAILED → steward relay → manager reads model: `order_status: "FAILED"`, history: ...→FAILED, reason: `sor_validated`

**Idempotency confirmed**: Replaying `validated=fail` after the fulfillment is already FAILED → `published: 0`, history entries stay at 3, `order_status: "FAILED"` unchanged.

### State at end of session

- All 4 apps running: manager (8081), steward (8082), external-mock (8084, DEMO_MODE=true), acknowledge (8083)
- Working tree: `order-manager`, `order-external-mock` rebuilt and running with fixes
- Files changed (uncommitted): `SorProcess.java`, `OrderPersist.java`, `OrderStatus.java`
- Steward module was rebuilt in prior session (steward-ack/tick/request flows have `model.instance -> header.instance`)
- All changes uncommitted per user instruction

## Memory References

- `project-order-system-mongodb` (referenced + updated)
- `virtual-threads-rpc` (referenced — `po.request(...).get()` pattern)
- `conv-serialization-gotchas` (referenced — MsgPack null dropping)
